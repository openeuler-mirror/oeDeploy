name: helm
description: auto deploy helm (default v3.9.0)
action:
  install:
    description: install helm
    tasks:
      - name: install helm
        playbook: install.yml
        scope: all
  uninstall:
    description: uninstall helm
    tasks:
      - name: uninstall helm
        playbook: uninstall.yml
        scope: all
